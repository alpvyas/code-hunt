<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    script(src="./javascripts/comment.js" type="module" defer)
    title Document
  body
      h1 Hello from Videos!
      form(method="post" action=`/posts/${video.id}/delete`)
        button(type="submit" class="videoDeleteBtn") Delete
      div
        ul
          each comment in comments
            li=comment.body
            form(method="post" action=`/posts/${video.id}/comments/${comment.id}/delete`)
              button(type="submit" class=`commentDeleteBtn${comment.id}`) Delete
