extends navbar
block content
    body
      .videoTitle
        h1(class="videoId" id=`${video.id}`) #{video.title}
        .languageImg
          if video.languageId === 6
            img(src='/images/c++.png' class='logo')
      .descriptionBox
          p(class="description") #{video.description}
      .videoLinkContainer
        a(class="videoLink" id=`${video.link}`)
          button(class="linkBtn") Go to Video!
      .addComment
        form.comment-form
          textarea(name="body" id="commentBody" required)
          button(type="submit") Add Comment
      .comments
      script(src="/javascripts/comments.js" defer)
            //- form(method="post" action=`/posts/${video.id}/delete`)
            //- button(type="submit" class="videoDeleteBtn") Delete
